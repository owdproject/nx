{
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Target project"
    },
    "name": {
      "type": "string",
      "description": "The name of the module package to install"
    }
  },
  "required": ["name"]
}
