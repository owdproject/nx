{
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Target project"
    },
    "name": {
      "type": "string",
      "description": "The name of the theme package to install"
    }
  },
  "required": ["name"]
}
